component calculate "Example component with external calculation logic";

pin in float in;
pin out float out;
function _;
license "GPL";
;;

#include "calculate.c"  // extern bestand met de echte logica

FUNCTION(_) {
    out = calculate(in);
}

